package com.mycompany.firstJava.ch04;

public enum Week {			// codegroup

	MONDAY,					// code
	TUESDAY,
	WEDNESDAY,
	THURSDAY,
	FRIDAY,
	SATURDAY,
	SUNDAY

	
	// : 열거형 타입. enum type.
	
	// 이넘타입 안에 들어가있는 것: enum상수 (상수라서 다 대문자,상수기때문에 변경 불가. 불러서 쓰는 것만 가능.)
	// 호칭 : week ( = int)
	
	
	
	
}
